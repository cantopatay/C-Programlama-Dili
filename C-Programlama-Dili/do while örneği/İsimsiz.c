#include<stdio.h>
 void main()
 {
 	int i=0;
 	do
 	{// önce i nin degeri arttýrýlýyor
 	//sonra ekrana merhaba dünya yazdýrýlýyor
 	printf("%5d:Merhaba Dunya\n",++i);
 	// yüzdeden sonra d'den önce gelen sayý
 	// yazýnýn biraz saða doðru yazýlmasýný saðlýyor
 	
	 }
	 while(i<25);
	 getch();
 }
